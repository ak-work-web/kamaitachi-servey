ja:
  errors:
    format: "%{message}" # デフォルトは "%{attribute}%{message}" なので、%{attribute}を消す

  activerecord:
    models:
      topic: "お題"
    attributes:
      topic:
        title: "タイトル"
        description: "説明"
    # errors:
    #   messages:
    #     blank: "を入力してください"
      # vote:
      #   user_id: "あなた"
    